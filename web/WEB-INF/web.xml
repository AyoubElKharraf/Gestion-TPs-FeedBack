<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
         http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
         version="3.1">

    <display-name>MiniProjet-BE4</display-name>

    <!-- ==================== CONFIGURATION GÉNÉRALE ==================== -->
    
    <!--
        URL du système externe Gestion Absences / AbsTrack.
        Si "Le système Absences n'a pas enregistré l'absence" :
        (1) Vérifier que cette URL pointe bien vers l'application AbsTrack (sans slash final).
        (2) Démarrer l'application AbsTrack sur le port indiqué (ex. 8081).
        (3) Dans AbsTrack, l'étudiant et l'enseignant doivent exister avec les MÊMES emails que dans EtudAcadPro.
        Voir CONFIG_ABSENCES.md pour le dépannage.
    -->
    <context-param>
        <param-name>absence.system.url</param-name>
        <!-- Projet Gestion Absences : ../ElKharrafMansouri_MiniProjet_JakartaEE → contexte déployé ci‑dessous (port 8081) -->
        <param-value>http://localhost:8081/ElKharrafMansouri_MiniProjet_JakartaEE/AbsTrack</param-value>
    </context-param>

    <!-- ==================== CONFIGURATION SÉCURITÉ API ==================== -->
    
    <!-- Activer/désactiver l'authentification API (true par défaut) -->
    <context-param>
        <param-name>api.key.enabled</param-name>
        <param-value>true</param-value>
    </context-param>
    
    <!-- Clé(s) API valide(s), séparées par virgule 
         IMPORTANT: Changer cette clé en production! -->
    <context-param>
        <param-name>api.key</param-name>
        <param-value>EtudAcadPro-API-2025-SecretKey,AbsTrack-Integration-Key</param-value>
    </context-param>
    
    <!-- Liste blanche d'IPs autorisées sans clé API (optionnel, séparées par virgule)
         Exemple: 127.0.0.1,192.168.1.10 pour autoriser localhost et un serveur -->
    <context-param>
        <param-name>api.allowed.ips</param-name>
        <param-value>127.0.0.1</param-value>
    </context-param>

    <!-- ==================== FILTRES ==================== -->
    
    <!-- Filtre d'authentification utilisateurs (sessions) -->
    <filter>
        <filter-name>AuthFilter</filter-name>
        <filter-class>util.AuthFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/vues/*</url-pattern>
    </filter-mapping>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
    </filter-mapping>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/enseignant/*</url-pattern>
    </filter-mapping>
    <filter-mapping>
        <filter-name>AuthFilter</filter-name>
        <url-pattern>/etudiant/*</url-pattern>
    </filter-mapping>

    <!-- Page d'accueil -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>
</web-app>